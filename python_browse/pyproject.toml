[project]
name = "python-browse-mcp"
version = "0.1.0"
description = "MCP server wrapping python-browse — terminal browser automation for AI agents"
requires-python = ">=3.13"
dependencies = [
    "mcp>=1.0.0",
    "python-browse",
    "pydantic>=2.0.0",
]

[tool.uv.sources]
python-browse = { path = "/home/nero/Documents/Estudos/07-tools-and-infrastructure/python_browse" }
