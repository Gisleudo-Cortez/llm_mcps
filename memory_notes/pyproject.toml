[project]
name = "memory-notes"
version = "0.1.0"
description = "MCP server for persistent key-value memory across sessions"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "mcp>=1.27.0",
]
