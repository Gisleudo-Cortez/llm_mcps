[project]
name = "awesome-lists"
version = "0.1.0"
description = "MCP server for browsing and searching the sindresorhus/awesome meta-list"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "mcp>=1.27.0",
]

[dependency-groups]
dev = [
    "pytest>=9.0.3",
]
