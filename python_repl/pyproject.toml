[project]
name = "python-repl"
version = "0.1.0"
description = "MCP server for executing Python code in an isolated subprocess"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "mcp>=1.27.0",
]
