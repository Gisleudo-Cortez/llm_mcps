[project]
name = "miku-voice"
version = "0.1.0"
description = "MCP server for text-to-Miku-voice synthesis via RVC pipeline"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "mcp>=1.26.0",
    "pydantic>=2.12.5",
]
