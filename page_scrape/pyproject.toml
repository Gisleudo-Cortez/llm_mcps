[project]
name = "page-scrape"
version = "0.1.0"
description = "MCP tool to fetch and scrape URL content"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "mcp>=1.26.0",
    "beautifulsoup4>=4.12.0",
    "lxml>=5.0.0",
    "trafilatura>=2.0.0",
    "requests>=2.33.1",
]
