[project]
name = "exa-search"
version = "0.1.0"
description = "MCP tools for Exa Search API — neural semantic search and similar-URL discovery"
requires-python = ">=3.13"
dependencies = ["mcp>=1.27.0", "requests>=2.32.0"]
