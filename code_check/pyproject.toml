[project]
name = "code-check"
version = "0.1.0"
description = "MCP server for code formatting and linting using the same tool chain as Neovim (conform.nvim + nvim-lint)"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "mcp>=1.27.0",
    "pydantic>=2.13.3",
]
